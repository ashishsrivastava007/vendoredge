# Test-only configuration. Never use these values for production.
# Point this at a dedicated test database, never the production database.
TEST_DATABASE_URL=postgresql://<test-user>:<test-password>@<test-host>:5432/<test-database>
